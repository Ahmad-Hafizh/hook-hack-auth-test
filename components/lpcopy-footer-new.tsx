// Commented out - using LPCopyFooterSimple instead
/*
export function LPCopyFooterNew() {
  const navigationLinks = [
    "機能",
    "[活用方法] デジタル広告/LP・記事LP/自社EC・Amazon/SNS運用/サイネージ・店頭販促",
    "料金プラン",
    "導入事例",
    "動画テンプレート",
    "ブログ",
    "セミナー",
    "お役立ち資料",
  ];

  const relatedServices = [
    {
      title: "運用型UGCで売上向上",
      name: "Letro",
    },
    {
      title: "結果を出すXキャンペーン",
      name: "echoes",
    },
    {
      title: "顧客の声をAIで解析",
      name: "Kaname.ax",
    },
    {
      title: "企業へのAI導入伴走支援",
      name: "Sherpa AI",
    },
    {
      title: "マーケティングメディア",
      name: "SMM Lab",
    },
  ];

  const footerLinks = [
    "運営会社",
    "個人情報保護方針",
    "情報の外部送信について",
  ];

  return (
    <footer className="bg-[#00c8c8] text-white">
      {/* Navigation Section */}
      <div className="border-b border-white/20">
        <div className="max-w-7xl mx-auto px-4 md:px-6 lg:px-8 py-6 md:py-8">
          <div className="flex flex-col md:flex-row items-start md:items-center justify-between gap-4 md:gap-8">
            {/* Logo */}
            <div className="text-xl md:text-2xl font-bold">HookHack</div>

            {/* Navigation Links */}
            <nav className="flex flex-wrap items-center gap-4 md:gap-6 text-sm md:text-base">
              {navigationLinks.map((link, index) => (
                <a
                  key={index}
                  href="#"
                  className="hover:text-yellow-300 transition-colors whitespace-nowrap"
                >
                  {link}
                </a>
              ))}
            </nav>

            {/* Contact Info */}
            <div className="text-sm md:text-base text-right">
              <div className="font-semibold mb-1">050-1790-7052</div>
              <div className="text-sm">受付時間/平日10:00~18:00</div>
            </div>
          </div>
        </div>
      </div>

      {/* Related Services Section */}
      <div className="border-b border-white/20">
        <div className="max-w-7xl mx-auto px-4 md:px-6 lg:px-8 py-8 md:py-12">
          <h3 className="text-xl md:text-2xl font-bold mb-6 md:mb-8">
            関連サービス
          </h3>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6 md:gap-8">
            {relatedServices.map((service, index) => (
              <div key={index} className="space-y-2">
                <div className="text-sm md:text-base text-white/80">
                  {service.title}
                </div>
                <div className="text-lg md:text-xl font-bold">
                  {service.name}
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Footer Links and Copyright */}
      <div className="max-w-7xl mx-auto px-4 md:px-6 lg:px-8 py-6 md:py-8">
        <div className="flex flex-col md:flex-row items-center justify-between gap-4">
          {/* Footer Links */}
          <div className="flex flex-wrap items-center gap-4 md:gap-6 text-sm md:text-base">
            {footerLinks.map((link, index) => (
              <a
                key={index}
                href="#"
                className="hover:text-yellow-300 transition-colors"
              >
                {link}
              </a>
            ))}
          </div>

          {/* Copyright */}
          <div className="text-sm md:text-base text-white/80">
            © 2023 Allied Architects, Inc.
          </div>
        </div>
      </div>
    </footer>
  );
} */

